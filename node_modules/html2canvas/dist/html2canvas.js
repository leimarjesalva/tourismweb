/* global define */
define(['jquery', 'canvas-to-image'], function ($, canvasToImage) {
  'use strict';

  function html2canvas(element, options) {
    return canvasToImage(element, options);
  }

  return html2canvas;
});