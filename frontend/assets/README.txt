Place your site logo image here as `logo.png` (recommended size ~360x360, web-optimized PNG).

This project expects the logo at: `frontend/assets/logo.png`.

You provided an attachment; please save it to the path above (overwrite if present) so the site will display the logo in the header, footer, and admin login.

If you prefer another filename, update the `<img src="assets/logo.png">` references in the following files:
- frontend/index.html
- frontend/admin_dashboard.html
- frontend/admin_login.html

Alternatively, you can embed a `data:` URI directly into those files if you want the image inlined.